Implementiere jetzt Phase A (MVP) aus deinem Plan: Workflow Connections + Meter-UI.

Scope Phase A:
1) DB / Schema:
- Lege neue Tabellen an:
  a) workflows: id, practiceId, name, actorType (patient|staff|instruments), createdAt
  b) workflow_connections: id, workflowId, fromRoomId, toRoomId, label (nullable), weight (default 1), createdAt
- Nutze Drizzle pgTable in shared/schema.ts + InsertSchemas + Types.

2) Storage + Routes:
- Ergänze server/storage.ts um CRUD:
  - listWorkflows(practiceId)
  - createWorkflow(practiceId, name, actorType)
  - listConnections(workflowId)
  - createConnection(workflowId, fromRoomId, toRoomId, label?, weight?)
  - deleteConnection(connectionId)
- Ergänze server/routes.ts API-Endpunkte:
  - GET /api/practices/:id/workflows
  - POST /api/practices/:id/workflows
  - GET /api/workflows/:id/connections
  - POST /api/workflows/:id/connections
  - DELETE /api/workflow-connections/:id

3) Client API:
- Ergänze client/src/lib/api.ts um workflows + connections calls.

4) LayoutEditor UI:
- Lade workflows (für current practice) + connections (für selected workflow).
- Wenn keine Workflows existieren: erzeuge automatisch einen Default-Workflow “Neupatient (Patient Flow)” actorType=patient.
- Baue einen Toggle “Connect Mode”.
- Connect Mode Interaction:
  - Klick auf Raum A setzt “fromRoom”.
  - Klick auf Raum B erstellt Connection A->B via API, cleared selection.
  - Zeige eine kleine UI-Hilfe (“Wähle Start-Raum…”, “Wähle Ziel-Raum…”).
- Zeichne Connections als SVG-Overlay über der Canvas:
  - Linie vom Center(fromRoom) zum Center(toRoom)
  - Pfeil-Markierung (marker-end)
  - Optional: label am Linien-Mittelpunkt
- Add Delete: Liste der Connections in Sidepanel + Trash Button.

5) Meter statt px:
- Definiere eine Konstante PX_PER_M = 40 (oder falls im Code schon vorhanden: verwende die bestehende Konstante aus benchmarks/units).
- Zeige in der UI Breite/Höhe als Meter (z.B. 3.0 m) statt “px”.
- Slider bleibt intern px, aber Anzeige ist Meter. (Kein Rewrite zu Meter-Speicherung.)
- Achte darauf, dass Drag weiterhin smooth bleibt. Das SVG-Layer darf keine Pointer Events blocken.

Regeln:
- Kleine PR-ähnliche Schritte, keine Rewrites.
- Jede Änderung: kurze Erklärung + betroffene Dateien + wie testen.
- Füge minimal sinnvolle Tests oder manuelle Testanleitung hinzu.

Am Ende:
- Gib mir eine kurze Checkliste “Manual QA” (Connect erstellen, Drag room, delete connection, meter display).